{
  "name": "obsidian-pages",
  "version": "1.0.0",
  "description": "Static site generator for Markdown files",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx http-server dist -p 8080",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "markdown-it": "^14.0.0",
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}